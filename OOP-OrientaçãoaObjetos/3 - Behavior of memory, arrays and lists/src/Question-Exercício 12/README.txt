(EN - US) Question Statement:

* Make a program to read an integer N and then the data (id, name and salary) ofN employees. There should be no repetition of id. Then effect an increase of X percent in the salary of a particular employee. To do this, the program must read an id and the value X. If the id does not exist, show a message and abort the operation. At the end, show the updated list of employees, according to the examples. Remember to apply the encapsulation technique to not allow the salary to be changed freely. be changed freely. A salary can only be increased based on a given
given percentage raise operation.

(PT - BR) Enunciado da Questão:

* Fazer um programa para ler um número inteiro N e depois os dados (id, nome e salario) deN funcionários. Não deve haver repetição de id. Em seguida, efetuar o aumento de X por cento no salário de um determinado funcionário. Para isso, o programa deve ler um id e o valor X. Se o id informado não existir, mostrar uma mensagem e abortar a operação. Ao final, mostrar a listagem atualizada dos funcionários, conforme exemplos. Lembre-se de aplicar a técnica de encapsulamento para não permitir que o salário possa ser mudado livremente. Um salário só pode ser aumentado com base em uma operação de
aumento por porcentagem dada.
(EN - US) Notes:

* The example problem can also be solved in the following way: count += (minutes - 100) * 2.0;
* a = a + b is the same as a += b; a = a - b is the same as a -= b; a = a * b is the same as a *= b; a = a / b is the same as a /= b; a = a % b is the same as a %= b;

(PT - BR) Observações:

* O problema do exemplo também pode ser resolvido da seguinte forma: conta += (minutos - 100) * 2.0;
* a = a + b é o mesmo que a += b; a = a - b é o mesmo que a -= b; a = a * b é o mesmo que a *= b; a = a / b é o mesmo que a /= b; a = a % b é o mesmo que a %= b;
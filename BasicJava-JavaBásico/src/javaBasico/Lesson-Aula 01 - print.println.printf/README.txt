(EN) Notes:

* %s = string; %d = integer; %f = floating-point number; %n = line spacing.

(PT-BR) Observações:

* %s = texto; %d = número inteiro; %f = número com ponto flutuante; %n = espaçamento entre linhas.